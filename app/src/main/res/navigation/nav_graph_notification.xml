<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/nav_graph_notification"
    app:startDestination="@id/fragment_notification">

    <!-- Postpone date/time picking is handled directly in NotificationAct via
         MaterialDatePicker/MaterialTimePicker (no dedicated destination needed) —
         ReminderPostponeDateDialog/ReminderPostponeTimeDialog below don't exist in the
         codebase. Safe Args still generated an action pointing at a nonexistent class;
         removed as dead nav graph nodes. FIX-L02. -->
    <fragment
        android:id="@+id/fragment_notification"
        android:name="com.mckimquyen.notes.ui.noti.NotificationFrm" />

</navigation>
