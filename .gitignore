dev/

# Publishing
/app/publishing.gradle

# Android Studio
local.properties
*.iml
.idea
.gradle
build/

# Signing secrets — keystore lives in myKeyStore private repo (see keystore.properties)
keystore.properties
keystore.jks
app/keystore.jks
gradle.properties.bak

# OS files
Thumbs.db
.DS_Store

# Claude Code local settings (YOLO/bypassPermissions — không commit)
.claude/settings.local.json

# Kotlin/Gradle daemon cache — session lock files churn every build, never track
.kotlin/
